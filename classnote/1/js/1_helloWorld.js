//document.write("<h2>HELLO WORLD DYNAMIC</h2>");

//a method belonging to the HTML DOM document object > puts anything within the parentheses to the browser.

// confused about > in addition to placing a script in the body of a document, you can also put ti in the head section. 

// The location of any JavaScript code, (i.e. within the head of body tags) is in teh default location on the page where JavaScript will write to.

<script type ="text/javascript">
    document.write('<h1>HELLO WORLD IN HEAD</h1>');
</script>


// order matters because HTML is parsed by the browser top-down

/* the DOM defines the rules by which an HTML document can be broken down:
    - each element in an HTML doc > broken down into discrete obj.
    - each ob its own properties and methods
    - due to the existence of the DOM - we can use JS to manipulate and alter then HTML elements in an HTML page
*/

